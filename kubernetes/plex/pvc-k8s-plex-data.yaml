apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-k8s-plex-data
  namespace: plex
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ""
  resources:
    requests:
      storage: 30Ti
